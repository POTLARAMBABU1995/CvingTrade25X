-- Oracle 19c schema for CvingTrade25X auth (registrations + login activity)

DECLARE
  v_count NUMBER;
BEGIN
  SELECT COUNT(*) INTO v_count FROM user_tables WHERE table_name = 'REGISTRATIONS';
  IF v_count = 0 THEN
    EXECUTE IMMEDIATE '
      CREATE TABLE REGISTRATIONS (
        ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        CLIENT_ID VARCHAR2(50) NOT NULL,
        FULL_NAME VARCHAR2(150) NOT NULL,
        EMAIL VARCHAR2(150) NOT NULL,
        MOBILE_E164 VARCHAR2(20) NOT NULL,
        DOB DATE NOT NULL,
        GENDER CHAR(1),
        PAN VARCHAR2(10),
        AADHAAR_LAST4 VARCHAR2(4),
        EXP_MONTHS NUMBER(5) DEFAULT 0,
        PASSWORD_HASH VARCHAR2(512) NOT NULL,
        PASSWORD_ALGO VARCHAR2(30) NOT NULL,
        MPIN_HASH VARCHAR2(512),
        MPIN_ALGO VARCHAR2(30),
        CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
        UPDATED_AT TIMESTAMP,
        METADATA_JSON CLOB,
        CONSTRAINT UQ_REGISTRATIONS_CLIENT UNIQUE (CLIENT_ID),
        CONSTRAINT UQ_REGISTRATIONS_EMAIL UNIQUE (EMAIL),
        CONSTRAINT UQ_REGISTRATIONS_MOBILE UNIQUE (MOBILE_E164)
      )';
  END IF;
END;
/

DECLARE
  v_count NUMBER;
BEGIN
  SELECT COUNT(*) INTO v_count FROM user_tables WHERE table_name = 'LOGIN_ACTIVITY';
  IF v_count = 0 THEN
    EXECUTE IMMEDIATE '
      CREATE TABLE LOGIN_ACTIVITY (
        ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        REGISTRATION_ID NUMBER NOT NULL,
        LOGIN_TIME TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
        LOGIN_IP VARCHAR2(50),
        LOGIN_DEVICE VARCHAR2(255),
        LOCATION VARCHAR2(120),
        METHOD_USED VARCHAR2(30),
        METADATA_JSON CLOB,
        CONSTRAINT FK_LOGIN_ACTIVITY_REG
          FOREIGN KEY (REGISTRATION_ID)
          REFERENCES REGISTRATIONS(ID)
      )';
  END IF;
END;
/

DECLARE
  v_count NUMBER;
BEGIN
  SELECT COUNT(*) INTO v_count FROM user_tables WHERE table_name = 'AUTH_SESSIONS';
  IF v_count = 0 THEN
    EXECUTE IMMEDIATE '
      CREATE TABLE AUTH_SESSIONS (
        ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        REGISTRATION_ID NUMBER NOT NULL,
        SESSION_TOKEN VARCHAR2(128) NOT NULL,
        CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
        LAST_SEEN_AT TIMESTAMP,
        EXPIRES_AT TIMESTAMP NOT NULL,
        LOGOUT_AT TIMESTAMP,
        LOGIN_IP VARCHAR2(50),
        USER_AGENT VARCHAR2(255),
        METADATA_JSON CLOB,
        CONSTRAINT FK_AUTH_SESSIONS_REG
          FOREIGN KEY (REGISTRATION_ID)
          REFERENCES REGISTRATIONS(ID),
        CONSTRAINT UQ_AUTH_SESSIONS_TOKEN UNIQUE (SESSION_TOKEN)
      )';
  END IF;
END;
/

DECLARE
  v_count NUMBER;
BEGIN
  SELECT COUNT(*) INTO v_count FROM user_tables WHERE table_name = 'AUTH_QUICK_MPIN';
  IF v_count = 0 THEN
    EXECUTE IMMEDIATE '
      CREATE TABLE AUTH_QUICK_MPIN (
        ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        REGISTRATION_ID NUMBER NOT NULL,
        TOKEN_HASH VARCHAR2(64) NOT NULL,
        CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
        EXPIRES_AT TIMESTAMP NOT NULL,
        LAST_USED_AT TIMESTAMP,
        REVOKED_AT TIMESTAMP,
        LOGIN_IP VARCHAR2(50),
        USER_AGENT VARCHAR2(255),
        METADATA_JSON CLOB,
        CONSTRAINT FK_AUTH_QUICK_MPIN_REG
          FOREIGN KEY (REGISTRATION_ID)
          REFERENCES REGISTRATIONS(ID),
        CONSTRAINT UQ_AUTH_QUICK_MPIN_TOKEN UNIQUE (TOKEN_HASH)
      )';
  END IF;
END;
/

DECLARE
  v_count NUMBER;
BEGIN
  SELECT COUNT(*) INTO v_count FROM user_indexes WHERE index_name = 'IDX_LOGIN_ACTIVITY_USER';
  IF v_count = 0 THEN
    EXECUTE IMMEDIATE 'CREATE INDEX IDX_LOGIN_ACTIVITY_USER ON LOGIN_ACTIVITY (REGISTRATION_ID)';
  END IF;
END;
/

DECLARE
  v_count NUMBER;
BEGIN
  SELECT COUNT(*) INTO v_count FROM user_indexes WHERE index_name = 'IDX_AUTH_QUICK_MPIN_REG';
  IF v_count = 0 THEN
    EXECUTE IMMEDIATE 'CREATE INDEX IDX_AUTH_QUICK_MPIN_REG ON AUTH_QUICK_MPIN (REGISTRATION_ID)';
  END IF;
END;
/

DECLARE
  v_count NUMBER;
BEGIN
  SELECT COUNT(*) INTO v_count FROM user_indexes WHERE index_name = 'IDX_AUTH_QUICK_MPIN_EXPIRES';
  IF v_count = 0 THEN
    EXECUTE IMMEDIATE 'CREATE INDEX IDX_AUTH_QUICK_MPIN_EXPIRES ON AUTH_QUICK_MPIN (EXPIRES_AT)';
  END IF;
END;
/
DECLARE
  v_count NUMBER;
BEGIN
  SELECT COUNT(*) INTO v_count FROM user_indexes WHERE index_name = 'IDX_AUTH_SESSIONS_REG';
  IF v_count = 0 THEN
    EXECUTE IMMEDIATE 'CREATE INDEX IDX_AUTH_SESSIONS_REG ON AUTH_SESSIONS (REGISTRATION_ID)';
  END IF;
END;
/

DECLARE
  v_count NUMBER;
BEGIN
  SELECT COUNT(*) INTO v_count FROM user_indexes WHERE index_name = 'IDX_AUTH_SESSIONS_EXPIRES';
  IF v_count = 0 THEN
    EXECUTE IMMEDIATE 'CREATE INDEX IDX_AUTH_SESSIONS_EXPIRES ON AUTH_SESSIONS (EXPIRES_AT)';
  END IF;
END;
/
